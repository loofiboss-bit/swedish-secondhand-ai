# Scripts

- `validate-release.mjs` checks the approved v1 version/tag, changelog, docs, and package targets.
- `package-smoke.mjs` verifies expected Linux/Windows artifacts, size, and executable headers.
- `generate-checksums.mjs` writes deterministic SHA-256 sums for release assets and the SBOM.
